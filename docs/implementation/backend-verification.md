# Backend verification

The current domain/unit suite and real PostgreSQL API suite exercise the ordinary manufacturing fixtures, state transitions, scope isolation, authorization, approval hashes, bounded retries and ERP commands. Latest output: [API tests](api-integration-output.txt) and [unit JUnit](../../evidence/test-results/domain-junit.xml). Connected HTTPS tests are separately recorded in [hosting](hosting.md).
