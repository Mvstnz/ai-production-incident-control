# Current contracts

Canonical data: ordinary steel rods, band saws and mounting plates, entirely synthetic. Supplier baseline: 40 rods missing / 83 CRITICAL; confirmed split: 10 missing / 56 HIGH; machine: 62 HIGH; quality: 70 CRITICAL with verified override.

Approval binds exact version and payload hash. Unknown delivery outcomes require reconciliation. Scopes isolate synthetic runs; the public viewer has access only to the shared demo. PostgreSQL is authoritative, and n8n orchestrates the stages.
